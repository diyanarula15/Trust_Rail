# Core API
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12
httpx>=0.27

# Data
SQLAlchemy>=2.0.36
alembic>=1.14
psycopg[binary]>=3.2
redis>=5.2

# Crypto
PyNaCl>=1.5
cryptography>=43

# Media (required)
Pillow>=11
imagehash>=4.3
pypdf>=5.1

# Text / claims
rapidfuzz>=3.10
confusable-homoglyphs>=3.3

# Email / DNS
dnspython>=2.7

# Type sniffing (POSIX only — see note below)
python-magic>=0.4.27; sys_platform != 'win32'

# Dev / test
pytest>=8.3

# OPTIONAL (feature-flagged; install manually if wanted):
#   pdqhash        — perceptual PDQ-256 hashing
#   c2pa-python    — C2PA manifest embed/read
#   anthropic      — LLM claim extraction (LLM_ENABLED=true)

# NOTE (Windows): python-magic wraps libmagic, which has no native Windows build.
# Unlike a missing import (caught cleanly), the package's ctypes lookup for
# libmagic.dll hangs indefinitely instead of raising — so it's excluded here by
# marker rather than left for the try/except in pipeline/ingest.py to catch.
# ingest.py's built-in signature sniffer is the verified fallback on Windows.
reportlab>=4.0  # fixture generation only (scripts/gen_fixtures.py)
